# VirtuaCity Nexus — Rebuilt Scaffold

This repo has been wiped and replaced with a minimal Next.js scaffold. Restore from the backup archive `../virtua-city-nexus-pre-wipe.zip` or branch `backup/pre-wipe` if needed.

Quick start:

```powershell
npm install
npm run dev
```
